<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="UnigetUI.MainApp"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:widgets="using:UnigetUI.Interface.Widgets"
    xmlns:local="using:UnigetUI">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls"/>
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            <!-- Other app resources here -->

            <Style TargetType="MenuFlyoutItem" x:Key="BetterMenuItem" BasedOn="{StaticResource DefaultMenuFlyoutItemStyle}">
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Height" Value="36"/>
            </Style>

            <Style TargetType="MenuFlyoutPresenter" x:Name="BetterContextMenu" BasedOn="{StaticResource DefaultMenuFlyoutPresenterStyle}">
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="BorderBrush" Value="{ThemeResource DividerStrokeColorDefaultBrush}"/>
            </Style>
            
        </ResourceDictionary>
    </Application.Resources>
</Application>
